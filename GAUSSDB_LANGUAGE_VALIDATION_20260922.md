# 中文设置修复与验证

日期：2026-09-22。适用版本：26.1.5 中文界面更新。

## 修复内容

旧交付包通过 Equinox 启动器启动，未执行 DBeaver 启动器读取 `settings/global-settings.ini` 的逻辑，因此界面已保存的 `nl=zh` 未参与下一次启动，而显式 `-nl zh` 可以生效。Linux 和 Windows 组装脚本现统一选择随产品构建的 DBeaver 启动器，并拒绝在交付配置中固定语言。

产品配置向导在语言变化后标记需重启；首次启动向导及帮助菜单入口均处理重启提示。补充产品配置向导、功能选择、GaussDB 调试参数面板、包编译提示的中文资源。随包加入 118 个对应现有 Eclipse 宿主插件的中文资源片段；原始资源、固定校验值和许可证随源码及产品提供，不替换 Eclipse 执行代码。

## 执行结果

| 场景 | 方法 | 结果 |
|---|---|---|
| 启动器配置 | LF/CRLF、重复运行、重复启动器、固定语言拒绝测试 | 4 项通过 |
| 中文资源 | 核对三个 NLS 类每个字段的中英文值；资源片段宿主筛选及重复安装 | 4 项通过 |
| 模块回归 | Maven 指定模块 verify | 71 个模块构建成功；673 个测试中 662 通过、11 跳过，零失败/错误；跳过项未计为通过 |
| 完整产品构建 | 208 模块产品构建，构建阶段跳过重复测试 | 通过；最终限定版本 202609220932 |
| 帮助菜单选中文 | 麒麟 V10 x86_64 用户空间、Xvfb 图形环境；英文启动，打开产品配置，选简体中文、应用、确认重启 | 无 `-nl` 参数时重启后菜单中文，配置为 `nl=zh` |
| 首次启动选中文 | 最终构建使用全新用户数据和工作区；配置向导选中文，选择不共享统计，应用、确认重启及退出 | 无 `-nl` 参数重启后为中文 |
| 配置向导中文 | 重启后打开“帮助 → 产品配置” | 标题、语言、提示、按钮中文；截图见使用说明书 |
| 调试窗口中文 | 打开调试、断点、变量、表达式视图；用自动化夹具打开生产参数面板 | 窗口、参数标题及列名中文；夹具模拟行不代表数据库调试验收 |
| 无自动化插件启动 | 干净交付目录读取同一语言配置，无 `-nl` 启动 | 中文菜单与调试视图正常显示；控制台有 GTK 滚动条布局警告，未阻断启动，不计为无告警 |

模块测试和产品构建未执行 Checkstyle/Spotless 检查。界面测试副本加载自动化插件；交付包不包含这些插件。既有数据库调试验收记录仍单独保留，本次不把翻译检查当成新一轮真库端到端验收。

## 使用方法与边界

解压到新目录，不覆盖旧配置文件。通过“帮助 → 产品配置 → 语言 → 简体中文”，完成向导、应用并重启。若原来在快捷方式或 `dbeaver.ini` 中固定了 `-nl en`，应移除该选项和语言值，否则命令行设置优先。

本次不承诺全部上游界面百分之百中文：较新的通用文本、每日提示正文、已有文件夹名称仍可能英文。SQL 关键字、数据库对象名及服务端诊断保留原文。Eclipse 社区资源包的版本早于当前平台，新增键可能回退到英文。

Windows 包沿用相同启动器和资源组装逻辑，仍需 Windows 原生环境验证。麒麟容器图形环境不等于客户桌面云现场验收。当前交付包为 x86_64，不适用于 ARM64 客户端。
