# 工行需求纠偏：DBeaver 26.1.5 迁移与 GaussDB 507 验证

日期：2026-09-21。用户同意本轮先使用 507 验证，不代表放弃原需求指定的金融版版本或 M 模式目标。

## 结论

本轮完成真正的 26.1.5 基线迁移、取消调试层对 M 标签的一刀切拒绝，并通过 507 支持环境的回归。**原始需求没有全部完成：本地 507 的 M 库拒绝创建函数、过程和包，无法完成 M 模式 PL/SQL 调试和包编译端到端验收。** 不以 ORA 测试通过替代 M 需求。

这是一条独立开发分支，不修改已发布 26.2.0 预览包，也没有将未验证版本重新发布到 GitHub。

## 1. 源码基线与已改内容

- 工作区：`/Users/lj/Documents/GaussDB/dbeaver-icbc-26.1.5`。
- 分支：`codex/icbc-gaussdb-26.1.5`。
- 起点为仓库真实标签 `26.1.5`，提交 `4cd7a4a1e6`，不是把现有 26.2 产品版本号改成 26.1.5。
- 将定制 GaussDB 适配及后续修复迁移到此基线，保留原 26.1.5 产品定义及上游源码。冲突为 GaussDB model/UI 插件版本元数据，保留已修复插件版本 1.0.48。
- 首次产品检查发现共享 dbeaver-common 继承了 26.2.0 标识/P2 仓库，已在此分支根 POM 显式固定 dbeaver-version=26.1.5，不修改共享依赖仓库、不影响原工作区。初轮 654 项真库回归在此属性锁定前完成；固定属性后的产品/回归结果单独记录，不隐去这个差异。
- `GaussDBDebugCore` 不再直接拒绝 M 标签；对于所有已识别模式，要求例程已持久化、OID 有效、实际语言为 plpgsql/plsql。启动时原有调试 API 签名、权限检查仍保留。
- `GaussDBBreakpointAdapterFactory` 使用同一例程资格检查，不再直接拒绝 M，同时补上普通 SQL/未知语言不能建立调试断点的检查。
- 回归断言覆盖 M + plpgsql/plsql 可进入后续能力检查；M + SQL/未知语言/未保存对象拒绝；未知模式、PG 和 ORA 原有边界保留。这里只证明客户端判断规则，不证明任何 M 服务端一定可调试。
- 存储过程创建和包能力仍保留既有限制，未在缺少服务端证据时贸然开放；这两项仍属于原始需求未闭环内容。

## 2. 本地 507 的 M 库：实际阻塞证据

环境：既有集中式实验容器 `gaussdb-507-ha-lab`，Kernel 507.0.0 build `d791c80a`。创建本轮专用 `icbc_m_probe_20260921`，DBCOMPATIBILITY='M'，使用实例本地管理账号通过原厂 gsql 执行。

目录查询结果：sql_compatibility=M；pg_language 中有 plpgsql；dbe_pldebugger 命名空间有 turn_on、attach、step、next、finish、断点和变量相关 API（含重载共 25 行）。**目录项存在不足以证明模式允许创建或调试对应例程。**

| 直接提交给数据库的语句 | 结果 |
|---|---|
| CREATE FUNCTION ... RETURNS integer LANGUAGE plpgsql AS ... | GAUSS-09049，SQLSTATE 42601，错误位置 FUNCTION |
| CREATE PROCEDURE ... AS BEGIN NULL; END | GAUSS-09049，SQLSTATE 42601，错误位置 PROCEDURE |
| CREATE PACKAGE ... AS FUNCTION ... RETURN integer; END | GAUSS-09049，SQLSTATE 42601，错误位置 PACKAGE |

每条测试都在独立 BEGIN/ROLLBACK 内执行。错误发生在原厂 gsql 直接执行时，与 DBeaver 菜单、驱动 URL、快捷键无关。使用管理账号也失败，不是缺少普通开发账号的 CREATE 授权所导致的当前现象。

复现脚本：[icbc-m-capability-probe.sql](test/org.jkiss.dbeaver.ext.gaussdb.debug.test/integration/icbc-m-capability-probe.sql)。只能对新建可删除的测试 M 库运行；包含目录查询和 DDL 探测。它是能力证据脚本，不会把服务端错误当作调试通过。

原始输出：`/tmp/icbc-507-m-capability-20260921.log`。没有改系统目录、修改数据库兼容模式、绕过服务器语法限制，或复制 ORA 对象来伪造 M 模式通过。

该结论仅适用于上述 build 和实测语句，不外推“所有金融版 M 都不支持”。若供应方有专门的 M 模式 PL/SQL 语法、版本或启用方式，需要据其准确说明再验证，不猜测开启参数。

## 3. 26.1.5 回归结果

完整选定 reactor 验证成功：658 项，654 通过、0 失败、0 错误、4 跳过。

```sh
JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-25.jdk/Contents/Home \
GAUSSDB_REVIEW_CONNECTION=/私有目录/connection.properties \
GAUSSDB_REVIEW_JDBC=/驱动目录/gsjdbc4.jar \
GAUSSDB_REVIEW_NATIVE=true \
mvn -o -fae verify -f product/aggregate/pom.xml \
  -pl "$(paste -sd, tools/gaussdb-review-reactor.txt)" \
  -Dskip-checkstyle=true -Dspotless.check.skip=true
```

日志：`/tmp/icbc-2615-live-final-20260921.log`。JDK 25 是构建/测试运行时，目标 Java 21 要求未降低。依赖仓库 dbeaver-common 固定为 `2df03e58480c6325732f476e8355e375cec5405e`。

| 范围 | 结果与边界 |
|---|---|
| M 调试入口资格/断点适配器 | 单测通过；没有伪称为 M 真库调试 |
| 集中式 ORA 包 ALL/SPEC/BODY 编译和错误行 | 507 真库通过；不是 M 包通过 |
| 调试会话断点增删启禁并发、真实 marker 删除 | 真库通过；不等于桌面云 GUI 验收 |
| COMMIT 回执丢失、无限读超时取消/关闭、重复提交保护 | 真实 JDBC + 故障代理通过 |
| 默认参数/重载歧义 | 真库目录验证通过 |
| 普通账号原生备份恢复、stdout 排空 | Linux 工具 + 生产管道传密方法通过，不是 Windows 原生工具验收 |
| PG 与平台回归 | 包含在 658 项中 |

4 个跳过为平台原有 SQL 补全 1 项、SVG 2 项和未准备独立空实例的 gs_dumpall 1 项。Checkstyle/Spotless 显式跳过。首轮夹具的深层 Mockito 语言 stub 不匹配已改为显式语言对象；首次真库连接缺少本轮访问规则，设置精确数据库/账号/转发 IP 规则后重跑。以上只统计最终成功运行。

## 4. 尚未闭环

### 产品版本锁定后的最终检查

- 完整 208 模块产品构建 BUILD SUCCESS，日志 `/tmp/icbc-2615-product-final-20260921.log`；macOS/Linux/Windows 两架构的六个 `.eclipseproduct` 均为 version=26.1.5，snapshot=26.1.5-202609210354。
- 根属性锁定后的独立回归再次 BUILD SUCCESS：658 项中 647 通过、11 跳过、0 失败/错误，日志 `/tmp/icbc-2615-pinned-regression-20260921.log`。此次数据库夹具已清理，7 个真库方法没有重跑，加原 4 个跳过共 11 个；不把它们计为通过，也不与前轮累加。
- 首次离线构建缺少 26.1.5 P2 元数据/依赖；经本机临时 Maven 代理配置从相应仓库补齐后完整构建成功，没有修改用户全局 Maven 配置。生产库/客户连接未用于构建测试。
- 产品输出在此工作区 `product/community/target/products`；这些是构建产物，不是已重新组装并完成麒麟桌面验收的客户交付包。没有替换旧 Release。

### 剩余阻塞

1. M 库 PL/SQL 和 Package 的服务端可执行前提；当前 507 无法完成正向样例创建，调试、包编译等相应端到端用例 BLOCKED。
2. M 存储过程创建/Package 能力发现、包内例程调试及包级变量等仍需目标服务端语义证据和实现，不能因调试入口不再硬拒绝 M 就宣布完成。
3. 505.2.1.SPC0800、503.1.0.SPC2000c 验收，本轮按用户要求使用 507，不计为已验证。
4. 新 26.1.5 产品在工行 x86 麒麟桌面云的安装、快捷键、UI 全链路及其他 ALL 部署组合，没有在本轮执行。

后续需要供应方确认这套 507 能否通过受支持配置提供所需能力；否则需一个能够创建客户 PL/SQL/Package 样例的环境。不能用客户端代码修复服务端缺失的语法/执行能力。

## 5. 环境清理

本轮创建的 4 个测试数据库、测试角色及私有凭据文件已删除，独立查询数据库/角色残留数均为 0；均为可重建的合成数据，不涉及客户数据。访问规则已恢复并核验 SHA-256 与原文件一致（e7bf4026e9e594463b92e6fc644b5aa311ff0bb2149c176e7dad8c26bc8a0060）。临时仅本机转发已停止并自动移除，集中式实验容器恢复停止状态，既有 CN 集群保持运行。
