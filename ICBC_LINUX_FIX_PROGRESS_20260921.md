# Linux GUI 三项问题修复进展（2026-09-21）

本记录对应 `ICBC_2615_LINUX_GUI_ACCEPTANCE_20260921.md` 中的问题，不替代原始验收记录。
**三个原始复现点已完成 Linux GUI + GaussDB 507 复验，并纳入 2026-09-21 r2 交付包**；不等于所有语法边界、全部平台或客户环境均已验收。后文按时间顺序保留验证过程。

复杂语法续测已完成一批并修复新发现的问题，见 [复杂语法验收报告](ICBC_COMPLEX_SYNTAX_ACCEPTANCE_20260921.md)。下文原有“独立 `/`、包初始化、IF/LOOP、多条 DROP/ALTER 未覆盖”的状态已由该报告更新。

## 本轮 GUI 验收结果（20:18—20:30）

环境：麒麟 V10 x86_64 容器、Xvfb/Metacity、DBeaver 26.1.5、GaussDB 507。宿主机为 ARM Mac，x86_64 通过 Rosetta 运行，不是客户桌面云实机。

| 场景 | 测试方法和实际结果 | 结论 |
| --- | --- | --- |
| 首次进入跨 schema 子过程 | 父过程第 4 行 F8 到第 5 行，再 F7 进入 `ui_cross_0909.ui_child`；栈显示第 4 行，正文 caretLine=4，只有一个子过程标签 | 通过 |
| 恢复旧编辑器 | 从原有测试工作区副本启动，子过程编辑器处于恢复状态；等待嵌套编辑器建立及正文加载后再返回真实输入 | 通过上述复现 |
| 断点反复启停 | 子过程第 5 行新增断点，断点窗格禁用/启用各两次；本次会话日志无断点异常；继续后命中第 5 行 | 通过 |
| 调用栈双击导航 | 子过程停在第 5 行，双击父帧定位父过程第 5 行，再双击子帧定位子过程第 5 行 | 通过 |
| 调试结束回滚 | 继续完成，点击生产事务对话框 Rollback；独立 SQL 查询审计表，前后均为 8 行、sum(value)=372 | 通过 |
| Package 脚本执行 | SQL 编辑器执行包声明、含嵌套函数的包体、SELECT，共 3 条语句；日志 Success，独立连接调用包函数返回 42 | 通过 |
| 整段选中执行 Package | 原生键盘全选，先确认 selection={0,104}；点击 Execute SQL query；日志保存 `END icbc_selected_0921;` 分号，Success，系统目录查到新包 | 通过 |

### 复测中继续修复的问题

第一轮等待只判断“已存在的源码编辑器是否加载完成”，将“尚无源码编辑器”误当成就绪，因此仍出现重复标签和第 1 行定位。
第二轮改成必须同时满足“嵌套源码编辑器已建立”和“正文已加载”，重新完整构建、重新组装测试客户端后，上表定位场景通过。
第一轮失败证据保留，不覆盖、不计为通过。

### 构建、测试与证据

- 最终第二轮产品构建：`/tmp/icbc-fix-product-r2-20260921.log`，BUILD SUCCESS。
- 最终第二轮 71 模块测试：`/tmp/icbc-fix-r2-tests-20260921.log`，BUILD SUCCESS。
- 证据归档：`/Users/lj/Documents/GaussDB/deliverables/icbc-linux-fix-20260921/`。
- `r1-queue/024.cmd.result`：三条 Package SQL 脚本执行。
- `r2-queue/010.cmd.result`、`first-child.png`：首次子过程第 4 行且只有一个编辑器。
- `r2-queue/013—018`：断点启停、继续命中；`breakpoint-reenabled.png`。
- `r2-queue/020.cmd.result`、`022.cmd.result`：父/子帧导航。
- `r2-queue/024.cmd.result`：提交/回滚对话框。
- `r2-queue/030.cmd.result`、`032.cmd.result`：完整选区及带末尾分号的执行日志。

### 清理与剩余边界

本轮创建的 `ui_pkg.icbc_fix_0921`、`ui_pkg.icbc_selected_0921` 已经通过管理员 SQL 删除，系统目录数量确认为 0；测试用例 SQL 文件保留，可重新创建。没有删除原有业务/测试包。此清理不算“GUI 批量删除”验收。
仍需独立补充 PostgreSQL 共享源码定位回归，以及独立 `/`、包初始化块、IF/LOOP、多条 DROP/ALTER PACKAGE、超时/关闭编辑器等边界。不把本次简单包通过扩大成全部 PL/SQL 语法通过。
本次未提交/推送代码，未上传新安装包。

## 权限恢复后的续测（20:14）

- 用户恢复全部权限后，Docker 访问恢复；两套 507 测试库分别执行 `SELECT 1` 成功。
- 最终工作区版本重跑 71 模块测试成功：`/tmp/icbc-fix-20260921-restored.log`，`BUILD SUCCESS`。
- 已组装新的 Linux GUI 测试副本，继续界面复验；下文权限中断为历史过程，不再是当前阻塞原因。

## 代码修改

1. **断点重复启用/禁用**：`GaussDBDebugSession` 在已有控制器锁内按已注册断点的状态判断，重复通知不再发送 SQL；真正的 SQL 异常仍向上传递，失败不修改本地状态，允许重试。
   新增不同描述符指向同一 OID/行、重复启停、失败后重试的单元测试。保留编号 0 支持。
2. **首次跨 schema 源码定位**：`DatabaseDebugModelPresentation` 在后台源码查找线程等待嵌套编辑器完成正文加载，再返回输入给 Eclipse 定位；不在 UI 线程等待，最多 30 秒，支持线程中断。
   该改动影响共享调试 UI，仍需 GaussDB 及 PostgreSQL GUI 回归。重复编辑器现象的原因和是否消失尚未验证；超时场景也不应视为成功定位。
3. **Package SQL 分割与末尾分号**：`GaussDBDialect` 增加 O 模式的 PL/SQL 块/包头识别及终止分号保留；M/PG 模式保持原有解析规则。
   新增包声明、含嵌套函数的包体、显式选中执行、普通 SQL、美元引号函数和 M/PG 事务语句解析测试。
   增加连接初始化期间数据库缓存为空的保护，避免查询默认数据库抛异常。

## 本轮已经执行的验证

- `/tmp/icbc-fix-20260921-r5.log`：71 模块测试 reactor，`BUILD SUCCESS`。新增 5 个方言测试和 2 个断点测试通过。
- `/tmp/icbc-fix-product-20260921.log`：完整产品构建，`BUILD SUCCESS`，包含数据库缓存为空保护。
- `git diff --check`：通过。
- 最后的全量测试重跑 `/tmp/icbc-fix-20260921-final.log`：因沙箱不能获取 `/Users/lj/.m2/repository/.meta/p2-artifacts.properties.tycholock` 而失败，未进入测试。这不是测试用例失败，亦不能算通过。

注意：成功的 r5 测试在最后加入数据库缓存为空保护之前；最终版本通过产品编译，但最终完整测试仍需重跑。

## 中断原因和待验收场景

运行期间环境由全访问切换为受限沙箱，Docker socket 返回 `permission denied`，且 Maven 缓存锁不可写；当前不允许申请提升权限。未绕过权限，也未将旧测试结果当成新代码 GUI 结果。

当时拟定的续测清单（完成状态以上方本轮结果为准）：

1. 重跑上述 71 模块测试。
2. 在麒麟 x86_64 GUI + GaussDB 507 上重复切换断点，检查服务端状态及工作区错误日志。
3. 关闭子过程编辑器后启动调试，首次 F7 跨 schema 进入时检查源码行、栈行、编辑器数量；继续检查双击调用栈、恢复/终止及 PostgreSQL 回归。
4. 用 SQL 编辑器执行包声明和包体，分别验证脚本执行、完整选择执行、函数返回结果及批量删除。
5. 继续覆盖独立 `/` 分隔符、包初始化块、IF/LOOP、注释/引号和多语句脚本；当前单测不能证明这些边界全部通过。
6. M 模式基本 SQL 与事务 GUI 回归；最终清洁包启动，再发布新的预览版本。不得覆盖历史预览的验证结论。

## 环境状态

最新状态：本轮 GUI 已正常退出；`dbeaver-kylin-x86_64`、`gaussdb-507`、`gaussdb-507-ha-lab` 已恢复停止。下述权限切换时未能停止属于历史状态。

本轮为复验启动了 `dbeaver-kylin-x86_64`、`gaussdb-507`、`gaussdb-507-ha-lab`，并启动 HA lab 的 55452 实例。
权限切换后无法访问 Docker，因此未能恢复它们原先的停止状态。未操作原本运行的 `gaussdb-507-cn-lab` 和其他业务容器。
未执行本轮 GUI 业务 SQL、未删除已有测试数据，未提交或上传本轮改动，未发布新的安装包。
