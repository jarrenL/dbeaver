# DBeaver 26.1.5 麒麟 Linux GUI 实测

日期：2026-09-21。结论：核心流程已实际运行，但本轮发现问题，**不能标记整体验收通过**。

## 环境与方法

- 分支 codex/icbc-gaussdb-26.1.5，包含本轮 O/M 范围澄清的未提交工作区修改，不是旧 26.2.0 发布包。
- 重新构建完整产品，208 模块 BUILD SUCCESS（39.759 秒）；窗口实际显示 DBeaver 26.1.5。
- 麒麟 V10 (Lance) 用户空间，x86_64 JRE 21、GTK/SWT、Xvfb + Metacity，普通 tester 用户。宿主 ARM Mac，x86_64 仿真；不是客户原生 x86 桌面云或完整麒麟内核认证。
- 用既有麒麟 JNI/启动器构建产物组装本次新产品；产品要求的 SWT/launcher 版本与组装脚本一致。测试副本加入由当前源码编译的 SWTBot；另保留不含测试插件的 clean 副本。
- 复制旧验收工作区至独立目录，验证旧配置恢复。不是全新账号首次安装验收。
- O/ORA 调试连接既有 GaussDB 507 的 dbeaver_ui_0909；包操作使用集中式 507 package_lab；M 使用集中式 dbeaver_ext_0909_m。
- GUI 命令的 OK 仅表示操作完成，以控件、日志、截图及独立 gsql 查询综合判定。SWTBot 快捷键注入存在未送达现象，部分组合键改用 X11 系统事件并核对文本选择范围。

## 场景及结果

证据目录：/Users/lj/Documents/GaussDB/deliverables/icbc-2615-linux-gui-20260921/evidence。下表数字为 queue 下命令/结果编号。

| 场景 | 实际测试方法与观察 | 结果 |
|---|---|---|
| 新版启动、配置恢复、连接 | 从原生启动器启动，窗口 26.1.5；恢复连接与过程源码（001–009） | 通过，限已有工作区 |
| 不含测试插件的客户端启动 | /opt/icbc-2615-clean，无SWTBot/acceptance bundle；独立复制工作区，原生启动器进入26.1.5主窗口（clean-start.png） | 启动及工作区恢复通过，不等于干净包重跑全部交互 |
| 参数与调试启动 | 调试配置 ui_acceptance.ui_parent(int4)，p_in=6，Debug 启用并暂停第4行（007–009） | 通过 |
| F8 | 第4行到第5行；变量 p_in=6，v_local=7（010–011） | 通过 |
| 改变量 | 变量菜单 Change Value，7改13，窗格回读13（013–016） | 通过 |
| Watch | 从变量菜单添加及显式添加表达式 v_local；父帧窗格实际显示7（017、038–045） | 通过变量名监视，不承诺表达式计算 |
| F7/跨 schema 调用栈 | 进入 ui_cross_0909.ui_child，栈为子4/父5，源码包含 +102（017–022） | 执行与堆栈通过；首次源码定位存在待确认问题 |
| 双击父帧 | 打开父源码并定位第5行（019–020） | 通过 |
| 新增断点及F9命中 | 在子正文第5行添加断点，F9命中子5/父5（023–028） | 通过 |
| 断点启停 | 窗格取消勾选/重新勾选；日志出现 enable_breakpoint invalid index/already enabled（029–030） | 不通过，不能把勾选变化当服务端成功 |
| Shift+F7 | 回到父第6行（030–031） | 通过 |
| Rollback | F9结束出现事务对话框，选择Rollback；独立查询仍6行、sum=256（032–034） | 通过 |
| 断点删除 | 从窗格 Remove；后续视图条目消失（048–050） | UI删除通过；本轮未做删除后同会话重加 |
| Commit | 下一轮完成，选择Commit；独立查询8行、sum=372，增加109+7（046–051） | 通过 |
| F10 | 新一轮父第4行终止，树显示terminated；独立查询仍8/372（053–056） | 通过 |
| 五模式能力模型 | 客户端实际加载 A/B/C/PG/M 五个库，M过程/包=false，仅A包=true（067） | 通过模型层，不替代全部UI入口验收 |
| 包ALL/SPEC/BODY | pkg_ui三种右键编译均显示success（073–078） | 通过 |
| 包错误定位 | boundary_body编译显示BODY第4行invalid type，双击错误后源码caretLine=4（079–082） | 行号导航通过；服务端报错指向BEGIN，实际缺失类型声明在前一行，未对报错语义重新映射 |
| SQL编辑器创建Package | Alt+X执行两个包的脚本被拆分；全选单个完整定义执行时，日志发送SQL又缺少END后的分号（086–095） | 不通过，需核对GaussDB方言分隔/去分号规则 |
| 批量删包 | 准备两个新专用包，但创建受上述SQL路径问题阻断 | 未完成，不引用旧版通过结果代替 |
| M连接 | 修改独立测试工作区中的连接至M库，Test Connection显示Connected (186ms)（098–103） | 通过；连接显示名仍为package_lab，实际目标已是M库 |
| M基本DDL/DML | GUI执行建表、INSERT、UPDATE、SELECT，截图行1/updated；独立gsql一致（108–112） | 通过 |
| M删除数据 | GUI DELETE再SELECT count，独立gsql为0（113–115） | 通过 |
| M树/创建/调试菜单限制 | 本轮有真实能力模型与此前单测；未逐个打开M库全部对象菜单 | 仍需GUI补验 |

## 发现的问题与测试干扰区分

1. **断点启用错误**：07:56:41日志记录 Enable session breakpoint - error，服务端说明 index 越界或已启用。发生在启停步骤；需定位是否重复事件、异步时序或状态缓存导致。本轮不下最终根因结论。
2. **跨 schema 首次定位异常**：子栈为第4行，但初次进入及随后双击子帧，正文caretLine=1，出现重复子编辑器；父帧定位正确，后续子第5行断点定位正常。需用全新编辑器/工作区隔离恢复状态及异步定位时序；暂不认证跨函数导航全通过。
3. **Package SQL脚本执行问题**：完整定义的末尾分号未保留；独立菜单ALTER PACKAGE COMPILE成功不代表CREATE脚本路径成功。保留GUI Query Manager记录和工作台日志供修复。

不作为产品缺陷的测试干扰：首次启动遗漏-vmargs，已退出并修正后重启；一次错误的测试view ID导致PartInitException；未激活Connections页时取右键菜单超时，激活后编译成功；普通账号不能在M public建表，改用专用schema授权后成功；结果标签关闭确认框需先确认才继续脚本执行。历史工作区日志和初次错误启动记录未删，以免制造日志全绿。

## 数据与交付边界

只使用既有合成验收库；调试审计表保留新增两条测试记录（109、7），未清空旧记录。M表sample先通过GUI删除测试行，独立查询count=0；之后精确DROP本轮sample和schema icbc_gui_m_0921，均成功（无CASCADE），不会保留额外schema授权。临时包精确名称查询计数0，未删除原有ui_pkg包。清理的合成DDL可由证据脚本重建。

本轮没有测试ARM64新版、客户金融版505/503、客户桌面云远程链路、全新账号首次配置或所有ALL部署组合；没有发布安装包，也没有把发现的问题宣称修复完成。

新版测试与干净客户端均已退出；本轮启动的麒麟x86客户端、507单实例与集中式实验容器在收尾时恢复停止状态，原来运行的CN实验集群未停止。测试副本和工作区留在容器中供复现，不作为交付包。主仓库代码未在本轮GUI测试期间修改，新增了本报告和相关文档链接。
